#pragma once
#include "Ball.hpp"

class Boundary
{
public:

	//inline static constexpr float X_BOUND = 1.4f - Ball::radius_ - 0.092f;
	//inline static constexpr float Z_BOUND = 0.7f - Ball::radius_ - 0.042f;
	Boundary();
	~Boundary();

protected:

private:

};
